import React from "react";
import "./style.css";
function Header() {
    return (
        <header className="header" id="colors">
        
            <h1>Memory Game</h1>
            <h2>Click on the colors, but don't click the same color twice</h2>
        </header>
    );
}
export default Header;